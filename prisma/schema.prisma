generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model PopupPhoto {
  id  String @id @default(cuid())
  url   Json @default("[]")
}
